import { HeaderTitle, HeaderWrapper } from "./headerElements";

const Header = () => {
    return ( <>
        <HeaderWrapper>
            <HeaderTitle>
                InternSahara
            </HeaderTitle>
        </HeaderWrapper>
    </> );
}
 
export default Header;